# assets/

1. `repo_robot.glb` — MOVE it here from `design_claude/assets/repo_robot.glb`
   (it was downloaded into the About kit by mistake). Keep the filename.
2. `hero-first-frame.png` — created later by the build (media guide §2);
   becomes the Omni Flash image-to-video input and the scrub poster.

License: CC Attribution 4.0 — credit ships in the site footer (spec §0).
